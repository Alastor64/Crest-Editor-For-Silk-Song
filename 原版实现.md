# 《空洞骑士：丝之歌》原版纹章实现分析

> 本文根据当前 Steam 游戏目录中的 `Assembly-CSharp.dll` 进行分析，描述的是本机当前游戏版本的类型结构与运行逻辑。

## 一、总体结构

原版纹章不是一个“把所有能力都保存在字段中”的单一对象，而是由以下几层共同实现：

```text
ToolCrest（纹章物品、图标、插槽）
    ├─ ToolCrestsData（存档：解锁状态、槽位上的工具）
    ├─ HeroControllerConfig（动作参数、能力开关、动画库）
    ├─ HeroController.ConfigGroup（攻击对象、伤害器、下劈组件）
    └─ HeroController / PlayMaker（根据纹章 ID 执行的特殊能力）
```

### 装备和应用流程

1. 每个原版纹章是一个 Unity `ToolCrest` ScriptableObject。
2. `GlobalSettings.Gameplay` 保存各个原版 `ToolCrest` 的引用。
3. `ToolItemManager.GetAllCrests()` 枚举纹章，`GetCrestByName(id)` 按内部 ID 查找纹章。
4. 装备纹章时，`ToolItemManager.SetEquippedCrest()` 将纹章 ID 写入：

   ```csharp
   PlayerData.CurrentCrestID
   ```

5. `HeroController.ResetAllCrestState()` 根据纹章 ID 重置纹章状态并切换对应的 `ConfigGroup`。
6. `HeroController.SetConfigGroup()` 将该组中的攻击对象、伤害器和 `HeroControllerConfig` 缓存到玩家控制器中。
7. 普通攻击、下劈、蓄力攻击等动作主要读取 `HeroControllerConfig` 和 `ConfigGroup`。
8. 野兽回血、收割者缚丝后状态、漫游者暴击等特殊能力还会检查当前装备的纹章 ID，并维护各自的独立状态。

因此，只复制一个纹章的 `HeroControllerConfig` 只能复制部分行为，不能自动复制该纹章的全部特色。

## 二、`ToolCrest`：纹章本体

`ToolCrest` 负责纹章的身份、名称、图片、插槽布局、升级关系和角色配置引用。

| 字段 | 类型 | 含义 |
|---|---|---|
| `name` / `nameCache` | `string` | 纹章内部 ID，例如 `Wanderer`、`Warrior`；存档、装备和特殊行为判断均依赖此 ID |
| `cachedName` | `bool` | `nameCache` 是否已经缓存，属于内部优化字段 |
| `displayName` | `LocalisedString` | 背包中显示的纹章名称 |
| `description` | `LocalisedString` | 纹章说明文字 |
| `itemNamePrefix` | `LocalisedString` | 获得物品等文本使用的名称前缀 |
| `getPromptDesc` | `LocalisedString` | 获得纹章时显示的提示描述 |
| `equipText` | `LocalisedString` | 装备相关提示文字 |
| `crestSprite` | `Sprite` | 背包中显示的主要纹章图像 |
| `crestSilhouette` | `Sprite` | 未选择、暗色或轮廓状态的图像 |
| `crestGlow` | `Sprite` | 选中纹章时使用的发光图像 |
| `displayPrefab` | `GameObject` | 纹章展示使用的预制体 |
| `slots` | `SlotInfo[]` | 工具插槽布局；数组长度通常就是槽位数量 |
| `heroConfig` | `HeroControllerConfig` | 此纹章对应的玩家动作参数和动画覆盖 |
| `isHidden` | `bool` | 是否从正常纹章列表隐藏；隐藏纹章在已装备时仍可能显示 |
| `previousVersion` | `ToolCrest` | 当前纹章的前一个升级版本 |
| `upgradedVersion` | `ToolCrest` | 当前纹章的升级版本 |
| `oldPreviousVersion` | `ToolCrest` | Unity 编辑器校验升级链时使用的旧引用 |
| `hasCustomAction` | `bool` | 背包界面是否提供该纹章的专属操作 |
| `customButtonCombo` | `Display` | 专属操作的按键、方向修饰和提示文字 |

### 重要计算属性

- `IsEquipped`：比较 `PlayerData.CurrentCrestID == name`。
- `IsUnlocked`：读取该纹章 ID 对应的存档数据。
- `IsBaseVersion`：当 `previousVersion == null` 时为真。
- `IsVisible`：综合升级版本、解锁状态和 `isHidden` 得出。
- `SaveData`：通过纹章内部 ID 从 `PlayerData.ToolEquips` 中取得。

## 三、`ToolCrest.SlotInfo`：插槽定义

每一个 `SlotInfo` 描述纹章图上的一个工具插槽。

| 字段 | 含义 |
|---|---|
| `Type` | 允许放入的工具类型：`Red`、`Blue`、`Yellow` 或 `Skill` |
| `Position` | 插槽在背包纹章图上的二维坐标 |
| `IsLocked` | 该插槽是否初始锁定 |
| `AttackBinding` | 攻击类工具的绑定方向：`Neutral`、`Up` 或 `Down` |
| `NavUpIndex` | 向上导航时优先到达的槽位索引 |
| `NavDownIndex` | 向下导航时优先到达的槽位索引 |
| `NavLeftIndex` | 向左导航时优先到达的槽位索引 |
| `NavRightIndex` | 向右导航时优先到达的槽位索引 |
| `NavUpFallbackIndex` | 向上的首选目标不可用时使用的备用索引 |
| `NavDownFallbackIndex` | 向下的首选目标不可用时使用的备用索引 |
| `NavLeftFallbackIndex` | 向左的首选目标不可用时使用的备用索引 |
| `NavRightFallbackIndex` | 向右的首选目标不可用时使用的备用索引 |

需要区分两种插槽数据：

- `ToolCrest.slots` 描述纹章资产中的槽位类型和 UI 布局。
- `ToolCrestsData.Data.Slots` 保存玩家实际解锁了哪些槽位，以及每个槽位当前装备的工具。

## 四、纹章存档数据

### `ToolCrestsData.Data`

| 字段 | 含义 |
|---|---|
| `IsUnlocked` | 玩家是否已经获得该纹章 |
| `DisplayNewIndicator` | 背包中是否显示“新获得”提示 |
| `Slots` | 该纹章每个插槽的运行时存档 |

### `ToolCrestsData.SlotData`

| 字段 | 含义 |
|---|---|
| `IsUnlocked` | 该插槽是否已经解锁 |
| `EquippedTool` | 装在该槽中的工具内部 ID；空字符串表示没有装备工具 |

## 五、`HeroControllerConfig`：动作参数

`HeroControllerConfig` 保存动作的数值、能力开关和动画覆盖，但不包含所有纹章特殊能力。

### 1. 普通攻击

| 字段 | 含义 |
|---|---|
| `attackDuration` | 一次普通攻击的持续时间窗口 |
| `attackRecoveryTime` | 攻击后恢复、重新允许移动或执行其他动作所需的时间 |
| `attackCooldownTime` | 普通攻击之间的冷却时间 |
| `quickAttackCooldownTime` | 快速攻击状态下的攻击冷却 |
| `quickAttackSpeedMult` | 快速攻击动画和斩击速度倍率 |
| `canTurnWhileSlashing` | 挥击过程中能否改变朝向 |
| `wallSlashSlowdown` | 墙上攻击时是否应用减速 |

野兽纹章使用派生类型 `HeroControllerConfigWarrior`，它还包含狂暴状态下的攻击参数：

| 字段 | 含义 |
|---|---|
| `rageAttackDuration` | 狂暴状态下的攻击持续时间 |
| `rageAttackRecoveryTime` | 狂暴状态下的攻击恢复时间 |
| `rageAttackCooldownTime` | 狂暴状态下的普通攻击冷却 |
| `rageQuickAttackCooldownTime` | 狂暴状态下的快速攻击冷却 |

该派生配置会在狂暴状态下通过属性覆盖普通攻击参数。

### 2. 能力开关

| 字段 | 含义 |
|---|---|
| `canBind` | 是否允许执行普通缚丝/回血动作 |
| `canBrolly` | 是否允许使用飘浮或伞类能力 |
| `canDoubleJump` | 是否允许二段跳 |
| `canHarpoonDash` | 是否拥有针矛冲刺能力 |
| `canNailCharge` | 是否允许蓄力攻击 |
| `canPlayNeedolin` | 是否允许演奏织忆弦针 |
| `forceBareInventory` | 是否强制使用无斗篷/特殊形态的背包、动画和相关表现 |

这些字段通常只控制“能否进入动作”。例如，`canBind` 并没有描述攻击回血、缚丝时对周围造成伤害等完整逻辑。

### 3. 蓄力攻击

| 字段 | 含义 |
|---|---|
| `chargeSlashChain` | 蓄力斩使用的连段或链式动作配置编号 |
| `chargeSlashLungeSpeed` | 蓄力斩向前突进的初速度 |
| `chargeSlashLungeDeceleration` | 蓄力斩突进的减速度 |
| `chargeSlashRecoils` | 蓄力斩是否会产生后坐力 |

其中部分字段可能由动画事件或 PlayMaker 间接读取，所以不一定能在普通 C# 调用链中看到直接引用。

### 4. 冲刺攻击

| 字段 | 含义 |
|---|---|
| `dashStabSpeed` | 冲刺刺击的移动速度 |
| `dashStabTime` | 冲刺刺击的持续时间 |
| `dashStabSteps` | 冲刺动作的阶段数或步数 |
| `dashStabBounceJumpSpeed` | 冲刺刺击反弹后的向上速度 |
| `forceShortDashStabBounce` | 是否强制使用短反弹流程 |

`canHarpoonDash` 则决定是否开放对应的冲刺能力。

### 5. 下劈与下冲

| 字段 | 含义 |
|---|---|
| `downSlashType` | 下攻击类型：`Slash`、`DownSpike` 或 `Custom` |
| `downSlashEvent` | 自定义下攻击触发的事件名 |
| `downspikeAnticTime` | 下冲动作的前摇时间 |
| `downspikeTime` | 下冲主体的持续时间 |
| `downspikeSpeed` | 下冲速度 |
| `downspikeRecoveryTime` | 下冲结束后的恢复时间 |
| `downspikeThrusts` | 是否采用持续推进式的下冲 |
| `downspikeBurstEffect` | 下冲时是否产生爆发效果 |

### 6. 动画

| 字段 | 含义 |
|---|---|
| `heroAnimOverrideLib` | 该纹章使用的玩家动画覆盖库，类型为 `tk2dSpriteAnimation` |

游戏通过 `HeroControllerConfig.GetAnimationClip(string)` 按动画名称从覆盖库中查找纹章专属动画。

## 六、`HeroController.ConfigGroup`：攻击实例

`HeroControllerConfig` 只保存数值和开关。真正的攻击碰撞体、伤害器和效果对象来自 `HeroController.ConfigGroup`。

| 字段组 | 内容 |
|---|---|
| `Config` | 当前组的 `HeroControllerConfig` |
| `ActiveRoot` | 激活这套动作对象的根节点 |
| `NormalSlash*` | 普通斩击、伤害器和 GameObject |
| `AlternateSlash*` | 备用普通斩击 |
| `UpSlash*` / `AltUpSlash*` | 上劈及备用上劈 |
| `DownSlash*` / `AltDownSlash*` | 下劈及备用下劈 |
| `Downspike` / `AltDownspike` | 下冲组件 |
| `WallSlash*` | 墙上攻击 |
| `ChargeSlash` | 蓄力攻击 GameObject |
| `DashStab` / `DashStabAlt` | 冲刺刺击及备用版本 |
| `TauntSlash` | 特殊或挑衅斩击对象 |

每个 `*Slash` 通常包含三个组成部分：

- `NailSlash`：斩击行为组件；
- `DamageEnemies`：伤害判定组件；
- `GameObject`：实际的场景对象。

这解释了为什么只复制攻击时间等参数是不够的：如果没有同时切换 `NormalSlashObject`、`NormalSlashDamager` 和动画库，攻击的外观、判定形状及伤害行为仍可能来自原来的纹章。

## 七、纹章 ID 与特殊能力

下列能力不是由单个 `HeroControllerConfig` 字段完整实现的：

- 野兽纹章的攻击回血和狂暴；
- 收割者纹章缚丝后的状态及丝球效果；
- 漫游者纹章的暴击；
- 猎手纹章的专注度或连击计量；
- 工匠纹章消耗丝线修补工具；
- 萨满纹章相关的法术效果；
- 巫女纹章缚丝时的范围攻击；
- 某些特殊上劈逻辑。

原版会为部分纹章维护独立的运行时状态：

| 状态结构 | 字段 |
|---|---|
| `WarriorCrestStateInfo` | `IsInRageMode`、`RageEffectTimeLeft`、`RageHealTimeLeft`、`RageModeHealCount`、`LastHealAttack` |
| `ReaperCrestStateInfo` | `IsInReaperMode`、`ReaperModeDurationLeft` |
| `HunterUpgCrestStateInfo` | `CurrentMeterHits`、`IsComboMeterAboveExtra` |
| `WandererCrestStateInfo` | `CriticalHitsLocked`、`QueuedNextHitCritical` |

这些行为通常通过以下入口判断纹章身份：

- `ToolCrest.IsEquipped`
- `HeroController.IsHunterCrestEquipped()`
- `HeroController.IsArchitectCrestEquipped()`
- `HeroController.IsShamanCrestEquipped()`
- PlayMaker 的 `CheckIfCrestEquipped`
- 其他按 `PlayerData.CurrentCrestID` 分支的代码

因此，如果自定义纹章使用一个新的内部 ID，原版代码不会自然地把它识别为野兽、收割者或漫游者纹章。Mod 必须在相应判断入口重定向身份，或者安全地重新实现相应效果。

## 八、自定义编辑器模块与原版数据的对应关系

| 编辑器模块 | 原版主要来源 |
|---|---|
| 插槽模块 | `ToolCrest`、`SlotInfo[]`、纹章图标和 HUD |
| 普通攻击模块 | `HeroControllerConfig`、`ConfigGroup`、动画覆盖库 |
| 回血方式模块 | 能力开关、纹章 ID、PlayMaker 和独立状态 |
| 蓄力攻击模块 | 蓄力配置字段、`ChargeSlash` 和动画 |
| 冲刺攻击模块 | 冲刺配置字段、`DashStab` 和动画 |
| 下劈跳模块 | 下劈配置字段、下劈/下冲组件和动画 |
| 回血后特效模块 | 纹章 ID、PlayMaker 和独立状态 |
| 上劈模块 | `ConfigGroup` 的上劈对象、伤害器、动画和部分身份逻辑 |
| 特殊技能模块 | 纹章身份判断及 `HeroController`/PlayMaker 专用代码 |

### “插槽模块”的特殊作用

编辑器中的“插槽模块”不只是决定有几个工具槽。按照当前设计，它还应作为合成纹章的外层 `ToolCrest` 模板，从而决定：

- 纹章主图、轮廓图和发光图；
- 插槽数量、类型和位置；
- 背包及 HUD 的基础视觉；
- 纹章展示预制体；
- 合成纹章在背包中的基础布局。

自定义名称和内部 ID 则应由 Mod 单独提供，不能直接复用原版纹章 ID，否则会与原版存档及装备判断冲突。

## 九、对当前 Mod 实现的结论

实现可组合纹章时至少需要处理四类内容：

1. **物品层**：克隆插槽来源纹章的 `ToolCrest`，设置新的内部 ID、名称和存档数据。
2. **配置层**：按模块复制来源纹章的 `HeroControllerConfig` 字段。
3. **对象层**：切换 `ConfigGroup` 中对应的斩击对象、伤害器和下冲组件。
4. **身份层**：重定向原版的纹章身份判断，或者为不适合直接进入原版状态机的能力提供独立实现。

只做第 2 层会出现以下问题：

- 数值改变了，但攻击判定和动画没有改变；
- 特殊回血方式不生效；
- 暴击、狂暴、修补工具等能力不生效；
- 原版状态机认为某个完整原版纹章已装备，但实际活动配置来自多个纹章，可能造成空引用或状态不一致。

当前项目中的字段分组位于 `src/SilksongHelper/Charm/CharmPart.cs`，运行时组合逻辑位于 `src/SilksongHelper/Game/CharmApplier.cs`。
